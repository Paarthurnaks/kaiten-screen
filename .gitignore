node_modules/
dist/
out/
release/
.env
.env.*
kaiten-screen.local-config.json
# Экспорт настроек из диалога "Сохранить в файл..." (Settings) может содержать реальный
# API-ключ в открытом виде — на всякий случай игнорируем любое имя с этим префиксом,
# если экспорт случайно попадёт в корень проекта.
kaiten-screen*config*.json
*.log
test-results/
playwright-report/
.idea/

# Внутренний справочник по Kaiten API с реальным доменом компании (alphacore.kaiten.ru) —
# не для публичного репозитория, оставляем только локально.
examples.md

# Личный черновик планов на будущее — не часть проекта, только для себя.
ROADMAP.local.md

# Служебные файлы AI-инструментов разработки (Claude Code / AI Factory) — не часть
# продукта, в публичном репозитории не нужны.
.claude/
.ai-factory/
.ai-factory.json
.mcp.json
AGENTS.md
skills-lock.json
